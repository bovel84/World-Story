import {describe,expect,it} from 'vitest';import {assessCapability,grantImportedCapabilities} from '../src/core/projects/CapabilityEngine';
const graph=[{id:'science',kind:'knowledge' as const,requires:[]},{id:'design_machine',kind:'design' as const,requires:['science']},{id:'manufacture_machine',kind:'manufacture' as const,requires:['design_machine']},{id:'operate_machine',kind:'operate' as const,requires:['science']},{id:'maintain_machine',kind:'maintain' as const,requires:['operate_machine']}];
describe('M05 µ3 — capability separate',()=>{it('tecnologia fondamentale assente blocca anche con fondi (MAT07)',()=>{expect(assessCapability(graph,new Set(), 'operate_machine')).toEqual({allowed:false,missing:['operate_machine','science']});});it('conoscenza presente ma fabbrica/capability assente non produce (MAT08)',()=>{const a=assessCapability(graph,new Set(['science','design_machine']),'manufacture_machine');expect(a).toMatchObject({allowed:false});expect(a.missing).toContain('manufacture_machine');});it('import concede operate/maintain dichiarati, mai manufacture (MAT23)',()=>{const g=grantImportedCapabilities(graph,new Set(['science']),['operate_machine']);expect(assessCapability(graph,g,'operate_machine').allowed).toBe(true);expect(assessCapability(graph,g,'manufacture_machine').allowed).toBe(false);expect(()=>grantImportedCapabilities(graph,g,['manufacture_machine'])).toThrow(/non concede/);});});
