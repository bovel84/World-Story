import {describe,expect,it} from 'vitest';import {allocateDailyFlow} from '../src/core/economy/FlowAllocator';
describe('M04 µ3 — flussi giornalieri',()=>{it('ordine stabile priority/queue e quota limitata dalla capacità',()=>{const r=allocateDailyFlow('100',[{projectId:'late',priority:2,queueSequence:1,requested:'80',eligible:true},{projectId:'first',priority:1,queueSequence:9,requested:'70',eligible:true},{projectId:'tie',priority:1,queueSequence:10,requested:'70',eligible:true}]);expect(r).toEqual([{projectId:'late',granted:'0',blocked:false},{projectId:'first',granted:'70',blocked:false},{projectId:'tie',granted:'30',blocked:false}]);});it('progetto bloccato non trattiene lavoratori/trasporto: la quota torna disponibile il giorno dopo',()=>{const day1=allocateDailyFlow('100',[{projectId:'p1',priority:0,queueSequence:1,requested:'100',eligible:false},{projectId:'p2',priority:1,queueSequence:2,requested:'100',eligible:true}]);expect(day1.map(x=>x.granted)).toEqual(['0','100']);const day2=allocateDailyFlow('100',[{projectId:'p1',priority:0,queueSequence:1,requested:'100',eligible:true},{projectId:'p2',priority:1,queueSequence:2,requested:'100',eligible:true}]);expect(day2.map(x=>x.granted)).toEqual(['100','0']);});it('non muta la domanda input',()=>{const d=[{projectId:'p',priority:0,queueSequence:0,requested:'10',eligible:true}];allocateDailyFlow('5',d);expect(d[0]?.requested).toBe('10');});});
