import {describe,expect,it} from 'vitest';import {cancelProjectFunding,createProjectFunding,restoreProjectFunding,spendProjectFunding} from '../src/core/projects/ProjectFunding';
describe('M05 µ5 — annullamento e costi sommersi',()=>{it('libera solo residuo e conserva costo già speso (MAT19)',()=>{const funded=createProjectFunding('reserve_phase','600');const after=spendProjectFunding(funded,'180');expect(after).toEqual({reservationId:'reserve_phase',initialReserved:'600',remainingReserved:'420',spent:'180',released:'0'});const cancelled=cancelProjectFunding(after);expect(cancelled).toMatchObject({releaseReservationId:'reserve_phase',releaseAmount:'420',sunkCost:'180',funding:{remainingReserved:'0',spent:'180',released:'420'}});});it('snapshot restore conserva residuo/speso; legacy ignoto non inventa fondi',()=>{const s=spendProjectFunding(createProjectFunding('r','100'),'40');expect(restoreProjectFunding(JSON.parse(JSON.stringify(s)))).toEqual(s);expect(restoreProjectFunding(undefined)).toBeNull();expect(restoreProjectFunding({reservationId:'r',initialReserved:'100',remainingReserved:'70',spent:'40'})).toBeNull();});it('non consente doppia spesa oltre riserva',()=>{expect(()=>spendProjectFunding(createProjectFunding('r','10'),'11')).toThrow(/oltre/);});});
