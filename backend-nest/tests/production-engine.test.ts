import {describe,expect,it} from 'vitest';import {planDailyProduction,validateExecutionRecipe} from '../src/core/economy/ProductionEngine';
const recipe={id:'steel',durationDays:3,inputs:[{resourceId:'ore',baseUnits:'6'},{resourceId:'coal',baseUnits:'4'}],outputs:[{resourceId:'steel',baseUnits:'5'},{resourceId:'slag',baseUnits:'1'}],energy:{resourceId:'electricity',baseUnitsPerUnit:'10'},labour:{qualification:'worker',minutesPerUnit:'30'},capacity:{unitsPerDay:'10'}};
describe('M04 µ1 — produzione fisica',()=>{it('bottleneck riduce output e consumi proporzionalmente (MAT17)',()=>{const r=planDailyProduction(recipe,'10',{resources:{ore:'60',coal:'12'},energy:{electricity:'100'},labourMinutes:{worker:'300'}});expect(r).toMatchObject({units:'3',energyConsumed:{baseUnits:'30'},labourMinutes:'90'});expect(r.consumed).toEqual([{resourceId:'ore',baseUnits:'18'},{resourceId:'coal',baseUnits:'12'}]);expect(r.produced).toEqual([{resourceId:'steel',baseUnits:'15'},{resourceId:'slag',baseUnits:'3'}]);expect(r.blockers).toContain('MATERIAL_SHORTAGE');});it('energia zero ferma produzione senza consumi (MAT16)',()=>{const r=planDailyProduction(recipe,'1',{resources:{ore:'6',coal:'4'},energy:{electricity:'0'},labourMinutes:{worker:'30'}});expect(r.units).toBe('0');expect(r.consumed.every(x=>x.baseUnits==='0')).toBe(true);expect(r.blockers).toContain('ENERGY_SHORTAGE');});it('rifiuta ricette senza dati esecutivi/durata positiva',()=>{expect(()=>validateExecutionRecipe({...recipe,durationDays:0})).toThrow(/durationDays/);expect(()=>validateExecutionRecipe({...recipe,energy:{resourceId:'',baseUnitsPerUnit:'1'}})).toThrow(/energia/);});});
