import {describe,expect,it} from 'vitest';import {allocateWorkforce,advanceResearch} from '../src/core/projects/PeopleResearchEngine';
describe('M05 µ4 — personale e ricerca',()=>{it('stesse persone non sono assegnate a formazione e produzione (MAT10)',()=>{const a=allocateWorkforce({worker:'10'},[{id:'production',qualification:'worker',requestedPersons:'7',priority:0,queueSequence:1},{id:'training',qualification:'worker',requestedPersons:'7',priority:1,queueSequence:2}]);expect(a).toEqual({production:'7',training:'3'});expect(BigInt(a.production??'0')+BigInt(a.training??'0')).toBe(10n);});it('milestone ha costi espliciti ed esito identico dopo restore/retry (MAT22)',()=>{const m={id:'research_a',requiredWork:'10',costMinorUnits:'50',successNumerator:'1',successDenominator:'2'};const initial={seed:'fixed-seed',milestoneId:'research_a',completedWork:'0',spentMinorUnits:'0',resolved:false};const a=advanceResearch(m,initial,'10','50');const b=advanceResearch(m,initial,'10','50');expect(a).toEqual(b);expect(a.state).toMatchObject({completedWork:'10',spentMinorUnits:'50',resolved:true});expect(()=>advanceResearch(m,a.state,'1','1')).toThrow(/non avanzabile/);});it('denaro senza lavoro non sblocca milestone',()=>{const m={id:'r',requiredWork:'10',costMinorUnits:'50',successNumerator:'1',successDenominator:'1'};const r=advanceResearch(m,{seed:'s',milestoneId:'r',completedWork:'0',spentMinorUnits:'0',resolved:false},'0','999');expect(r.state).toMatchObject({completedWork:'0',resolved:false,success:undefined,spentMinorUnits:'999'});});});
