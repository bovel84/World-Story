import {describe,expect,it} from 'vitest';import {applyPhysicalMovements,deliverShipment,departShipment,loseShipment} from '../src/core/economy/TransitEngine';
const base={id:'contract_1',resourceId:'steel',quantity:'20',ownerRef:'seller',originRef:'seller',destinationRef:'buyer',carrierRef:'carrier',departureDate:'1951-01-01',arrivalDate:'1951-01-04',status:'planned' as const};
describe('M04 µ4 — transito §7.2',()=>{it('buyer resta a zero fino alla consegna, poi conservazione 30kg (MAT14)',()=>{const start=departShipment(base,'1951-01-01','30',true);let stock=applyPhysicalMovements({seller:'30',buyer:'0','transit:contract_1':'0'},start.movements);expect(stock).toEqual({seller:'10',buyer:'0','transit:contract_1':'20'});expect(deliverShipment(start.shipment,'1951-01-03').movements).toEqual([]);const delivery=deliverShipment(start.shipment,'1951-01-04');stock=applyPhysicalMovements(stock,delivery.movements);expect(stock).toEqual({seller:'10',buyer:'20','transit:contract_1':'0'});expect(deliverShipment(delivery.shipment,'1951-01-04').movements).toEqual([]);});it('blocco prima della partenza conserva seller30/transit0/buyer0',()=>{const blocked=departShipment(base,'1951-01-01','30',false);expect(blocked.shipment.status).toBe('blocked');expect(blocked.movements).toEqual([]);});it('perdita in transito elimina 20 senza ricreazione e retry è no-op',()=>{const started=departShipment(base,'1951-01-01','30',true);let stock=applyPhysicalMovements({seller:'30',buyer:'0','transit:contract_1':'0'},started.movements);const lost=loseShipment(started.shipment);stock=applyPhysicalMovements(stock,lost.movements);expect(stock).toEqual({seller:'10',buyer:'0','transit:contract_1':'0'});expect(loseShipment(lost.shipment).movements).toEqual([]);});});
