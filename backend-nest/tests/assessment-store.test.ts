import {describe,expect,it} from 'vitest';import {AssessmentStore,StaleAssessmentError} from '../src/core/feasibility/AssessmentStore';
describe('M03 µ4 — preview anchor/TTL',()=>{it('restituisce assessment solo con anchor identico',()=>{let now=100;const s=new AssessmentStore<string>(()=>now,50);const a={gameId:'g',branchId:'b',checkpointId:'c',revision:2,queueVersion:3};s.put('x',a,'preview');expect(s.getFresh('x',a).value).toBe('preview');expect(()=>s.getFresh('x',{...a,queueVersion:4})).toThrow(StaleAssessmentError);});it('TTL, revisione e ramo invalidano senza mutare stato canonico',()=>{let now=0;const s=new AssessmentStore<number>(()=>now,10);const a={gameId:'g',branchId:'b',revision:1,queueVersion:1};s.put('x',a,7);now=11;expect(()=>s.getFresh('x',a)).toThrow(/stale/);s.put('y',a,8);expect(()=>s.getFresh('y',{...a,branchId:'child'})).toThrow(StaleAssessmentError);s.put('z',a,9);expect(()=>s.getFresh('z',{...a,revision:2})).toThrow(StaleAssessmentError);});});
