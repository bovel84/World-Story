import {describe,expect,it} from 'vitest';import {releaseOutputs,tickExtractionDay,tickWorkDay} from '../src/core/economy/WorkEngine';
const recipe={id:'mine',durationDays:2,inputs:[{resourceId:'fuel',baseUnits:'1'}],outputs:[{resourceId:'ore',baseUnits:'5'}],energy:{resourceId:'power',baseUnitsPerUnit:'2'},labour:{qualification:'miner',minutesPerUnit:'10'},capacity:{unitsPerDay:'3'}};
const avail={resources:{fuel:'10'},energy:{power:'10'},labourMinutes:{miner:'100'}};
describe('M04 µ2 — lavoro e confini',()=>{it('output di D non è disponibile prima del confine D+1',()=>{const t=tickWorkDay({completedUnits:'0',totalUnits:'2',pending:[]},recipe,'1951-01-01',avail);expect(t.production.units).toBe('2');expect(releaseOutputs(t.state,'1951-01-01').released).toEqual([]);expect(releaseOutputs(t.state,'1951-01-02').released).toEqual([{resourceId:'ore',baseUnits:'10'}]);});it('estrazione riduce deposito solo per output di lavoro reale e lo limita',()=>{const t=tickExtractionDay({depositId:'d',resourceId:'ore',remainingDeposit:'7',completedUnits:'0',totalUnits:'3',pending:[]},recipe,'1951-01-01',avail);expect(t.production.units).toBe('1');expect(t.production.produced).toEqual([{resourceId:'ore',baseUnits:'5'}]);expect(t.state.remainingDeposit).toBe('2');expect(releaseOutputs(t.state,'1951-01-02').released).toEqual([{resourceId:'ore',baseUnits:'5'}]);});it('zero energia non consuma deposito né crea output',()=>{const t=tickExtractionDay({depositId:'d',resourceId:'ore',remainingDeposit:'7',completedUnits:'0',totalUnits:'3',pending:[]},recipe,'1951-01-01',{...avail,energy:{power:'0'}});expect(t.state.remainingDeposit).toBe('7');expect(t.state.pending).toEqual([]);});});
